import icons from './theme/icons'

export const getDefaultUIConfig = () => ({
  icons
})

export const defaultOptions = {
  prefix: 'M',
  fonts: true,
  colorMode: true,
  theme: {
    transitions: true
  },
  mdc: false,
  content: false
}
